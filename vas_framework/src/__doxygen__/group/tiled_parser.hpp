/** @addtogroup vas_framework
	  @{
*/

/** @defgroup tile_codec Tiled Parser
	  @brief Simple Tiled TMX map parsing solution.

	  Tiled Parser is the built-in Tiled TMX parser to read .tmx file. The library also provide a simple TMX map rendering solution
	  for the game projects.

	  Only avaliable when pugixml library and zlib library found.
*/

/** @} */ //group vas_framework